@model Podmlotek.ViewModels.ProductsViewModel

<nav id="topnav">
	<ul class="menu">
		@foreach (var category in Model.Categories)
		{
			<li>
				<a href="@Url.Action("ProductList","Home",
				new { categoryId = category.CategoriesId
				})">
					@category.Name
				</a>
			</li>

		}
	</ul>
</nav>